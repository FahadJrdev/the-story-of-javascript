// Browser-only (document.write). comments.js - 1995
// A note for humans. The computer skips it.
/* A longer note,
   over several lines. */
document.write("Notes are ignored.")  // a note at the end of a line
