// A function is a recipe card: a name, some blanks to fill in, and steps
function greet(name) {
  return "Hello, " + name + "!"
}

alert(greet("Brendan"))
alert(greet("Ada"))
alert(greet())            // forgot to fill in the blank
